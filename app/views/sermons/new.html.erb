<%= form_for @sermon, url: {action: "create"} do |f| %>
  <% binding.pry %>
  <%= f.hidden_field :sermon_author_id %>
  <%= f.text_field :title %>
  <%= f.label :passage %>
  <%= f.text_field :passage %>
  <%= f.submit "Create" %>
<% end %>
